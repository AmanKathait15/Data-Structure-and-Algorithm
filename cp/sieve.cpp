#include<bits/stdc++.h>
using namespace std ;
#define num unsigned int

bool prime[1000000];
mmset(prime,1,sizeof(prime[0]));

void prime(num n)
{
    for(num i=2; i<)
}

int main()
{
    num n;
    cin>>n;
    
    for(num i=2; i<=n; i++)
    if(prime(i))
    cout<<i<<",";
    return 0;
}